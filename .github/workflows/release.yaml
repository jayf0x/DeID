# Desktop releases are built locally via `bun run release:desktop`.
# This workflow is a no-op kept for reference only.
name: Release Desktop
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Releases are published locally. Run 'bun run release:desktop' from the repo root."
